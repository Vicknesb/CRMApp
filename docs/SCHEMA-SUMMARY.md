# Schema Summary (generated after EPIC-DB)

> **Status: NOT YET GENERATED**
> This file is populated automatically when EPIC-DB completes (ticket DB-15).
> Until then, agents should read `apps/api/prisma/schema.prisma` directly.
>
> After DB-15 runs, this file will contain a one-line-per-model summary
> (model name, key fields, relations) so subsequent epic agents can load
> this ~100-line file instead of the full 600+ line schema.prisma.

---

## How to regenerate
After any schema migration, run:
```
Implement DB-15
```
The DB-15 ticket generates the ERD + data dictionary and should also update this file.
